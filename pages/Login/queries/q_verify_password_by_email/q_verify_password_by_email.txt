SELECT 1
FROM "User"
WHERE lower(trim(email)) = lower(trim({{ this.params.email }}))
  AND "businessId" = {{ Auth.businessId() }}::uuid
  AND password = crypt({{ this.params.currentPassword }}, password)
LIMIT 1;
