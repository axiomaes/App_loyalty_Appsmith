{
  "gitSyncId": "68f1ea66301c3b6f3e13c5f4_32680c06-ba2f-4099-844a-1bf127c2d2b5",
  "id": "Dashboard_q_bond_has_payment",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "WITH v AS (\n  SELECT\n    {{ this.params.customerId }}::uuid                                          AS cid,\n    COALESCE(NULLIF(TRIM({{ this.params.period || '' }}::text), ''),\n             to_char(now(), 'YYYY-MM'))                                         AS per\n),\nb AS (\n  SELECT b.\"id\" AS bond_id\n  FROM \"Bond\" b\n  WHERE b.\"customerId\" = (SELECT cid FROM v)\n  ORDER BY b.\"updatedAt\" DESC\n  LIMIT 1\n),\nhit AS (\n  SELECT 1 AS ok\n  FROM \"BondPayment\" bp\n  WHERE bp.\"bondId\" = (SELECT bond_id FROM b)\n    AND bp.period    = (SELECT per FROM v)\n  LIMIT 1\n)\nSELECT\n  (SELECT bond_id FROM b) IS NOT NULL     AS has_bond,\n  (SELECT COALESCE(MAX(ok), 0) FROM hit)  = 1 AS paid,\n  (SELECT per FROM v)                     AS period,\n  (SELECT bond_id FROM b)                 AS bond_id;\n",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "AxiomaDB",
      "isAutoGenerated": false,
      "name": "AxiomaDB",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "q_bond_has_payment",
    "pageId": "Dashboard",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}